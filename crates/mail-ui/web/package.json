{
  "name": "mailcore-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Le front de mailcore. Ouvrable tel quel dans un onglet, servi par le demon.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:tauri": "tsc --noEmit && vite build --mode tauri",
    "dev:tauri": "vite --mode tauri",
    "preview": "vite preview",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@tauri-apps/api": "^2.11.1",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vite-plugin-solid": "^2.11.0"
  },
  "dependencies": {
    "solid-js": "^1.9.0"
  }
}
